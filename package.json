{
  "name": "comoresnews",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "test": "node --test",
    "build": "node build/generate.mjs",
    "migrate": "node build/migrate.mjs"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.112.1",
    "gray-matter": "^4.0.3",
    "markdown-it": "^14.1.0",
    "sharp": "^0.35.3"
  },
  "devDependencies": {
    "turndown": "^7.2.0"
  }
}
